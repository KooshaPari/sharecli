CLUSTER_START cluster=C11 repo=sharecli pillars=L108-L122 date=2026-07-08

### L108 — Native Installer(s)
score: 1  glyph: △
evidence:
  - Cargo.toml:10-17 — cargo-dist + macOS bundle metadata (icon/category)
  - .github/workflows/release.yml:1-11 — comments claim macOS `.app` / Windows `.exe` tray archives
  - .github/workflows/release.yml:46-66 — actual job is ubuntu `cargo build` only (no dmg/msi/AppImage artifacts)
gaps:
  - Produce real per-OS installers in release CI (dmg/msi/deb or verified .app bundles) — effort: L
soft_goal_delta: packaging metadata seeded; native installer artifacts not evidenced

### L109 — CLI Packaging & Install Channels
score: 2  glyph: ~
evidence:
  - README.md:23-28 — `cargo install sharecli` / `cargo binstall sharecli` one-liners
  - Formula/sharecli.rb:1-12 — Homebrew formula stub (`sha256 = "PLACEHOLDER"`)
  - Cargo.toml:2-3 — package name/version for crates.io publish
  - .github/workflows/release.yml:97-119 — crates.io publish job
gaps:
  - Fill Homebrew sha256/version; prove crates.io publish succeeds on a tagged release — effort: M
soft_goal_delta: cargo channel documented; brew still placeholder

### L110 — Tray / Menubar Client
score: 3  glyph: ✓
evidence:
  - desktop/ShareCLITray/Sources/ShareCLITray/main.swift:46-49 — NSStatusItem tray registration
  - windows/ShareCLITray/ShareCLITray.csproj — Windows tray project present
  - crates/sharecli-tray-linux/src/main.rs:229 — Linux tray poll loop / StatusNotifier path
  - crates/sharecli-tray-linux/README.md:1-33 — Linux tray host requirements documented
gaps:
  - none identified for presence of multi-OS tray clients — effort: S
soft_goal_delta: tray clients exist on macOS/Windows/Linux trees

### L111 — Auto-Update
score: 0  glyph: ✗
evidence:
  - grep returned 0 results for `self-update|Sparkle|appcast|tauri-updater|auto.?update` in src/Cargo.toml
  - README.md:19-87 — install paths are manual cargo/release download only
gaps:
  - Add signed self-update or documented updater channel — effort: L
soft_goal_delta: not started

### L112 — Code-Signing & Notarization
score: 0  glyph: ✗
evidence:
  - grep returned 0 results for `codesign|notarytool|signtool|Authenticode` in .github/workflows/
  - .github/workflows/release.yml:17 — comment says “Builds, signs, and publishes” but no signing steps present
gaps:
  - Wire Developer ID + notarization / Authenticode in release pipeline — effort: L
soft_goal_delta: not started

### L113 — Deploy Surface: Container / OCI
score: 1  glyph: △
evidence:
  - Containerfile:1-8 — multi-stage Rust builder → debian runtime with ENTRYPOINT
  - .containerignore — present at repo root
  - Containerfile:5-8 — no `USER` non-root and no `HEALTHCHECK`
gaps:
  - Run as non-root + HEALTHCHECK hitting `/healthz`; document `container`/compose run — effort: M
soft_goal_delta: Containerfile seeded; hardened OCI incomplete

### L114 — Deploy Surface: Serverless / Edge
score: 0  glyph: ✗
evidence:
  - NOT_APPLICABLE: sharecli is a local process supervisor/CLI+tray — no Vercel/Workers web product surface claimed
  - MISSING: wrangler.toml / vercel.json
gaps:
  - none — not applicable
soft_goal_delta: n/a

### L115 — Deploy Surface: Traditional Server
score: 0  glyph: ✗
evidence:
  - NOT_APPLICABLE: no self-hosted reverse-proxy product path; supervisor binds locally per README API section
  - MISSING: Caddyfile / nginx.conf / systemd unit
gaps:
  - none — not applicable
soft_goal_delta: n/a

### L116 — Deploy-Surface Parity & Robustness
score: 1  glyph: △
evidence:
  - docs/CROSS_DEVICE_DEPLOY.md:1-66 — fleet multi-device install/register runbook
  - Containerfile:1-8 + README.md:169-180 — container + HTTP API surfaces exist
  - MISSING: surface matrix with per-target proof (deploy URL / build log)
gaps:
  - Publish docs/deploy.md matrix (CLI/tray/container/fleet) with proof links — effort: M
soft_goal_delta: multi-surface docs seeded; parity proofs incomplete

### L117 — Mobile Presence Decision
score: 0  glyph: ✗
evidence:
  - grep returned 0 deliberate mobile/PWA/Expo decision docs in docs/ or README.md
  - docs/CROSS_DEVICE_DEPLOY.md covers multi-device desktop/CLI fleet only
gaps:
  - Record explicit “no mobile app — CLI/tray/desktop only” rationale (scores 3 when deliberate) — effort: S
soft_goal_delta: unconsidered absence

### L118 — Release Automation
score: 2  glyph: ~
evidence:
  - .github/workflows/release.yml:45-119 — build/test → SLSA attest → crates.io publish automation
  - justfile:159-163 — `just changelog` via git-cliff
  - CHANGELOG.md:1-20 — Keep a Changelog + SemVer policy header
gaps:
  - Tag→multi-OS GitHub Release assets (installers/checksums) not fully automated — effort: M
soft_goal_delta: crates.io+attestation automated; installer asset matrix incomplete

### L119 — Versioning & Compatibility Policy
score: 2  glyph: ~
evidence:
  - CHANGELOG.md:6-9 — explicit Semantic Versioning adherence
  - Cargo.toml:3 — `version = "0.3.0"`
  - VERSION file present at repo root
  - MISSING: `rust-version` / MSRV declaration in Cargo.toml
gaps:
  - Declare MSRV and breaking-change section discipline in releases — effort: S
soft_goal_delta: SemVer documented; MSRV policy absent

### L120 — Distribution Channels & Discoverability
score: 2  glyph: ~
evidence:
  - README.md:19-35 — cargo install + Homebrew + releases page pointers
  - Formula/sharecli.rb:1-5 — homepage + release URL
  - .github/workflows/release.yml:97-119 — crates.io publish path
gaps:
  - Badges + proven brew/crates.io listing; winget/scoop optional — effort: M
soft_goal_delta: primary channels documented; ecosystem completeness partial

### L121 — Install/Uninstall Cleanliness & QOL
score: 2  glyph: ~
evidence:
  - src/config.rs:381 — `dirs::config_dir()` for config location
  - src/serve_lock.rs:118-120 — `$XDG_RUNTIME_DIR` lock directory convention
  - README.md:61-67 — cargo install to `~/.cargo/bin`
  - MISSING: uninstall docs/script in README.md / scripts/
gaps:
  - Document uninstall + data/config removal with consent — effort: S
soft_goal_delta: XDG/dirs conventions present; uninstall QOL thin

### L122 — Deploy Observability & Health Endpoints
score: 3  glyph: ✓
evidence:
  - src/commands/serve.rs:170-175 — `/healthz`, `/health/processes`, `/metrics/prometheus` routes
  - README.md:177-180 — documents `/health` and Prometheus metrics endpoints
  - Cargo.toml:30-32 — `tracing` + `tracing-subscriber` structured logging stack
  - src/commands/serve.rs:185-187 — Ctrl-C shutdown path for operator control
gaps:
  - none identified for health+metrics baseline — effort: S
soft_goal_delta: health/metrics/tracing present; OTEL export soft-goal open

CLUSTER_TOTAL score=19/45 pct=42% grade=D
CLUSTER_DONE cluster=C11 repo=sharecli
