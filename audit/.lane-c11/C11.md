CLUSTER_START cluster=C11 repo=sharecli pillars=L108-L122 date=2026-07-18

### L108 — Native Installer(s)
score: 3  glyph: ✓
evidence:
  - .github/workflows/release.yml — linux + macOS + Windows CLI matrix (`x86_64-pc-windows-msvc` zip)
  - .github/workflows/release.yml — packages `sharecli-*.tar.gz` / `.zip` + sha256 per target
  - .github/workflows/packaging-soft.yml — unsigned `.deb` build on `ubuntu-24.04` (phase 3)
  - .github/workflows/packaging-soft.yml — `dmg-msi-soft` job + `assert_dmg_msi_soft.sh` (phase 3.5)
  - scripts/packaging/build_deb.sh — `dpkg-deb` packaging → `dist/sharecli_*_amd64.deb`
  - scripts/packaging/build_dmg_layout.sh — unsigned `.app` Contents layout (no codesign)
  - scripts/packaging/build_msi_layout.sh + wix/sharecli.wxs — unsigned MSI staging (no signtool)
  - tests/c11_l108_dmg_msi_packaging.rs — FR-003 gate for soft dmg/msi scaffolds
  - docs/ops/dmg-msi-packaging.md — phase 3 + 3.5 DONE; signed path deferred L112
  - Cargo.toml — cargo-dist + macOS bundle metadata present
  - docs/deploy/FINALITY.md — multi-OS parity floor (archives + unsigned deb)
gaps:
  - Signed dmg/msi + attach on tag — effort: L (L112 Blocked)
soft_goal_delta: unsigned `.deb` + soft dmg/msi layouts in CI; signed classic installers still open (L112)

### L109 — CLI Packaging & Install Channels
score: 3  glyph: ✓
evidence:
  - README.md — per-host install blocks (macOS / Windows / Linux / WSL) + cargo install / binstall
  - Formula/sharecli.rb — bottle / head install paths
  - docs/deploy.md — documents brew + release + FINALITY
  - .github/workflows/release.yml — crates.io publish path retained
gaps:
  - Publish Formula to a tap / Homebrew core PR — effort: M
soft_goal_delta: bottle digest landed; tap publish still soft

### L110 — Tray / Menubar Client
score: 3  glyph: ✓
evidence:
  - desktop/ShareCLITray — NSStatusItem tray + DashboardView
  - windows/ShareCLITray/ShareCLITray.csproj — Windows WinUI tray
  - windows/ShareCLITray/App.xaml.cs — single-instance mutex `Local\ShareCLITray.SingleInstance`
  - windows/ShareCLITray/app.manifest — `asInvoker` + DPI; embedded via `ApplicationManifest`
  - crates/sharecli-tray-linux — Linux StatusNotifier path
  - .github/workflows/release.yml — attach tray/desktop archives on tag (Win soft)
  - docs/ops/win-tray-hardening.md — phase 2 runtime harden DONE; CI `continue-on-error` open
gaps:
  - Harden WinUI tray CI to remove continue-on-error — effort: M
soft_goal_delta: mutex + manifest embedded; Win tray CI soft until green builds

### L111 — Auto-Update
score: 2  glyph: ◯
evidence:
  - docs/ops/auto-update.md — soft channel matrix (crates.io / binstall / brew / GH Releases + sha256)
  - docs/ops/in-binary-updater.md — soft plan: self-update + Sparkle/WinUI + TUF metadata sketch (deferred)
  - docs/deploy.md — Auto-update (soft) section links L111 + in-binary roadmap
  - src/commands/upgrade.rs — soft probe (`UpgradeChannel` enum + `probe()` + `check()` CLI handler); 4 channels, no network egress
  - src/main.rs — `Commands::Upgrade { check, channel }` clap subcommand wired (no install path; soft contract)
  - tests/c11_l111_soft_upgrade.rs — 6 tests FR-003 (probe + channel parsing + semver + docs + CLI wiring)
gaps:
  - Signed self-update or Sparkle/WinUI appcast after L112 secrets — effort: L
soft_goal_delta: soft probe ships; in-binary self-update / Sparkle / WinUI appcast deferred to L112 + TUF pipeline (in-binary-updater.md)

### L112 — Code-Signing & Notarization
score: 2  glyph: ◯
evidence:
  - .github/workflows/codesign.yml — `macos-sign` hard gate: import-codesign-certs → codesign → notarytool submit --wait → stapler staple → codesign --verify --deep
  - .github/workflows/codesign.yml — Infisical-backed Apple secrets (brew install infisical CLI → `infisical export` → GITHUB_ENV masked), zero raw GitHub secrets
  - .github/workflows/codesign.yml — `windows-sign` Authenticode via AzureSignTool (`continue-on-error: true` until Azure KV provisioned)
  - docs/ops/codesign-notarize.md — hard-gate runbook: 5 Infisical keys (`APPLE_CERTIFICATE`/`APPLE_CERTIFICATE_PWD`/`APPLE_ID`/`APPLE_TEAM_ID`/`APPLE_APP_PASSWORD`) + `INFISICAL_TOKEN` secret + project/env vars
  - docs/deploy.md — Releases row: macOS signed+notarized, Windows unsigned (Azure KV pending)
  - .github/workflows/codesign.yml — `macos-sign` PROVEN through codesign (run 34027528219): Infisical `--plain` fetch → native p12 decode (30,683 B) → `apple-actions/import-codesign-certs` via `p12-filepath` → zig 0.14.1 install → `cargo build --release` (7m05s) → `codesign --sign "Developer ID Application: Koosha Paridehpour (GCT2BN8WLL)"` → `replacing existing signature` (REAL signature, not ad-hoc) → `notarytool submit --wait`
gaps:
  - Prove a signed+notarized+stapled macOS artifact on a release tag — effort: M (codesign SUCCEEDS; notarize blocked on a valid `APPLE_APP_PASSWORD` app-specific password — Apple 401 on the current value; supply correct password OR switch to API-key flow with `APPLE_API_KEY_ID=239UYVWCSV` + issuer UUID)
  - Windows Authenticode — effort: L (Azure Key Vault not provisioned)
soft_goal_delta: Infisical-backed macOS hard gate PROVEN through real Developer ID codesign; L112 stays **2** — scored 2 because notarize+staple still pending a valid Apple app-specific password (or API-key/issuer); INFISICAL_TOKEN is set and the fetch/import/codesign stages run clean

### L113 — Deploy Surface: Container / OCI
score: 3  glyph: ✓
evidence:
  - Containerfile:1 — multi-stage Rust builder → debian runtime
  - Containerfile:14 — `USER sharecli` non-root
  - Containerfile:17 — `HEALTHCHECK` curls `http://127.0.0.1:9000/healthz`
  - docs/deploy.md:24 — container smoke via podman build/run documented
gaps:
  - none identified for non-root + HEALTHCHECK baseline — effort: S
soft_goal_delta: hardened OCI present; cosign/SBOM soft

### L114 — Deploy Surface: Serverless / Edge
score: 1  glyph: ~
evidence:
  - NOT_APPLICABLE (seeded): docs/deploy.md — edge/serverless N/A by design for local supervisor
gaps:
  - none — not applicable
soft_goal_delta: matrix + deploy.md seed N/A (score 1)

### L115 — Deploy Surface: Traditional Server
score: 3  glyph: ✓
evidence:
  - docs/deploy/systemd/sharecli.service — sample systemd unit (loopback serve)
  - scripts/packaging/build_deb.sh — installs unit to `lib/systemd/system/sharecli.service`
  - .github/workflows/packaging-soft.yml — asserts systemd unit inside unsigned `.deb`
  - docs/deploy/caddy/Caddyfile.sample — reverse-proxy TLS terminator sample
  - docs/deploy/systemd/sharecli.service.md — install sketch + `dpkg-deb` inspect path
  - tests/c11_l115_systemd_packaging.rs — FR-003 gate (deb + CI + doc)
gaps:
  - Signed distro packages after L112 secrets — effort: L
soft_goal_delta: unsigned `.deb` ships first-class systemd unit; signed packages deferred

### L116 — Deploy-Surface Parity & Robustness
score: 3  glyph: ✓
evidence:
  - docs/deploy.md:1 — surface matrix with status/how/proof per target
  - docs/deploy.md — Homebrew **proven** (sha256 v0.3.0); GH Releases **ready**; OCI **ready**; `.deb` soft CI proof
  - docs/CROSS_DEVICE_DEPLOY.md:1 — fleet multi-device runbook retained
  - .github/workflows/packaging-soft.yml — deb artifact proof for Linux native installer row
gaps:
  - Attach signed dmg/msi on tag — effort: L (L112)
soft_goal_delta: matrix rows proven for primary channels; signed installers deferred

### L117 — Mobile Presence Decision
score: 3  glyph: ✓
evidence:
  - docs/adr/0001-no-mobile-app.md:1 — accepted ADR: no mobile/PWA/Expo
  - docs/adr/0001-no-mobile-app.md:17 — deliberate non-goal with rationale
  - docs/deploy.md:16 — Mobile row N/A linking ADR
gaps:
  - none identified for deliberate decision — effort: S
soft_goal_delta: explicit no-mobile decision complete

### L118 — Release Automation
score: 3  glyph: ✓
evidence:
  - .github/workflows/release.yml — CLI matrix (linux/mac/win) → trays → SLSA attest → crates.io
  - .github/workflows/release.yml — `github-release` attaches UNSIGNED archives + sha256 on `v*` / dispatch
  - justfile — `just changelog` via git-cliff; parity recipes `build-cli*` / `build-tray*`
  - CHANGELOG.md — Keep a Changelog + SemVer policy header
  - docs/deploy/FINALITY.md — lanes A/B/C finality (no GA overclaim for tray)
  - docs/ops/win-tray-hardening.md — Win tray release phases (unsigned zip → harden → sign L112)
gaps:
  - Signed/notarized assets (L112) still deferred — effort: L
soft_goal_delta: tag→GH Release attach for CLI + trays (Win soft); unsigned + checksums; tray runtime hardened

### L119 — Versioning & Compatibility Policy
score: 3  glyph: ✓
evidence:
  - CHANGELOG.md:6 — explicit Semantic Versioning adherence
  - Cargo.toml — `version = "0.3.0"` + `rust-version = "1.89"` (MSRV; matches File lock APIs)
  - VERSION:1 — version file at repo root
gaps:
  - Breaking-change section discipline in CHANGELOG for 1.0 — effort: S
soft_goal_delta: SemVer + MSRV declared (score-3)

### L120 — Distribution Channels & Discoverability
score: 3  glyph: ✓
evidence:
  - README.md — CI + crates.io + GitHub release badges; install one-liners
  - README.md:19 — cargo install + Homebrew + releases pointers
  - Formula/sharecli.rb:17 — homepage + release URL + head install path
  - docs/deploy.md:11 — channel status matrix for discoverability
gaps:
  - Homebrew tap publish; winget/scoop optional — effort: M
soft_goal_delta: primary channels documented + badges; tap publish still soft

### L121 — Install/Uninstall Cleanliness & QOL
score: 3  glyph: ✓
evidence:
  - README.md — Uninstall section with cargo/brew/manual removal + `sharecli uninstall`
  - src/commands/uninstall.rs — `sharecli uninstall` + `--purge-data` / `--dry-run`
  - src/paths.rs — well-known config/state/runtime paths
  - tests/c11_uninstall.rs — FR-003 CLI acceptance
  - src/config.rs:381 — `dirs::config_dir()` for config location
  - src/serve_lock.rs:118 — `$XDG_RUNTIME_DIR` lock directory convention
gaps:
  - none identified for documented uninstall + XDG conventions — effort: S
soft_goal_delta: `uninstall` subcommand shipped; data-export before purge still soft

### L122 — Deploy Observability & Health Endpoints
score: 3  glyph: ✓
evidence:
  - src/commands/serve.rs:170 — `/healthz`, `/health/processes`, `/metrics/prometheus` routes
  - README.md:177 — documents health and Prometheus metrics endpoints
  - Containerfile:17 — HEALTHCHECK hits `/healthz`
  - docs/openapi/serve.yaml:1 — OpenAPI stub for health/metrics/config surface
  - Cargo.toml:30 — tracing + tracing-subscriber structured logging stack
gaps:
  - none identified for health+metrics baseline — effort: S
soft_goal_delta: health/metrics/tracing + OpenAPI stub present; OTEL export soft

CLUSTER_TOTAL score=41/45 pct=91% grade=A
CLUSTER_DONE cluster=C11 repo=sharecli
