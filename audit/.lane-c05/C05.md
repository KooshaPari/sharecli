CLUSTER_START cluster=C05 repo=sharecli pillars=L41-L50 date=2026-07-11

### L41 — Structured logs
score: 2  glyph: ~
evidence:
  - src/main.rs — tracing_subscriber registry + fmt layer (NO_COLOR / verbose)
  - src/commands/serve.rs — #[instrument] on serve::run and HTTP handlers
  - src/otel.rs — optional OpenTelemetryLayer when OTLP endpoint env is set
gaps:
  - Default JSON (or logfmt) across all binaries; keep pretty fmt for local dev only — effort: M
soft_goal_delta: serve request/lifecycle spans instrumented; unified JSON policy still incomplete

### L42 — OpenTelemetry instrumentation
score: 2  glyph: ~
evidence:
  - Cargo.toml — opentelemetry 0.32 + opentelemetry_sdk 0.32.1 + opentelemetry-otlp + tracing-opentelemetry 0.33
  - src/otel.rs — OTLP/HTTP SpanExporter + SdkTracerProvider when OTEL_EXPORTER_OTLP_ENDPOINT set
  - docs/ops/otel.md — enable/export/propagation contract
gaps:
  - Cross-binary spans (CLI→IPC→tray) + metrics via OTel MeterProvider — effort: L
soft_goal_delta: one crate path wired (serve/tracing → OTLP); full SDK surface soft

### L43 — Metrics emission
score: 3  glyph: ✓
evidence:
  - src/commands/serve.rs — GET /metrics/prometheus route
  - src/http_red.rs — RED counters + latency histogram (requests/errors/duration buckets)
  - src/commands/serve.rs — process/health gauges + RED series in one exposition
  - unit tests in http_red.rs + serve prometheus tests
gaps:
  - USE (utilization/saturation/errors) for host/thermal — effort: M
soft_goal_delta: scrape path + RED HTTP + process gauges complete for serve

### L44 — Distributed trace propagation
score: 2  glyph: ~
evidence:
  - src/otel.rs — TraceContextPropagator installed (W3C)
  - src/commands/serve.rs — http_observability_middleware extracts/injects `traceparent`
  - docs/ops/otel.md — inbound/outbound propagation table
gaps:
  - Propagate across CLI, IPC, and tray boundaries — effort: L
soft_goal_delta: one HTTP boundary complete; multi-hop soft

### L45 — Continuous profiling
score: 2  glyph: ~
evidence:
  - src/pprof_http.rs — `GET /debug/pprof/profile` pprof protobuf (SHARECLI_PPROF=1)
  - Cargo.toml — unix `pprof` 0.15 with prost-codec (no flamegraph/inferno)
  - src/commands/serve.rs — route wired behind serve Bearer auth
  - docs/ops/profiling.md — enable/capture/Windows-external sampler notes
  - docs/openapi/serve.yaml — `/debug/pprof/profile` OpenAPI path
gaps:
  - Continuous Pyroscope push agent + Windows in-process sampler — effort: L
soft_goal_delta: opt-in HTTP pprof present on Unix; always-on agent soft

### L46 — Error budgets + SLOs
score: 2  glyph: ~
evidence:
  - docs/ops/SLO.md — draft SLOs for /healthz, /readyz, metrics scrape
  - docs/ops/SLO.md — RED metric names mapped for HTTP latency/error burn
gaps:
  - Burn-rate alerts + signed on-call error-budget policy — effort: M
soft_goal_delta: draft SLOs + probe mapping present; alerting automation absent

### L47 — Health endpoints
score: 3  glyph: ✓
evidence:
  - src/commands/serve.rs — /healthz + /readyz + /health/processes
  - unit tests for healthz/readyz JSON contracts
gaps:
  - Align README path naming (/health vs /healthz) if docs still drift — effort: S
soft_goal_delta: k8s-style liveness + readiness + process health complete

### L48 — Alerting routing
score: 2  glyph: ~
evidence:
  - docs/ops/alertmanager/sharecli.yml — Prometheus rules (critical/warning/info)
  - docs/ops/alerting.md — severity→PagerDuty/Slack/null routing + runbooks
  - src/notifier.rs — desktop + webhook notification dispatch
  - docs/ops/SLO.md — error-budget policy linked from alerting docs
gaps:
  - Live Alertmanager receiver secrets + signed on-call roster in-repo — effort: M
soft_goal_delta: severity-routed rule pack + runbooks present; live PD wiring soft

### L49 — Dashboard coverage
score: 2  glyph: ~
evidence:
  - docs/ops/grafana/sharecli-serve.json — Prometheus RED + process panels
  - src/commands/serve.rs — HTML dashboard at /
  - desktop tray DashboardView.swift — local UI
gaps:
  - Lane/org Grafana folders + provisioning as code — effort: M
soft_goal_delta: per-service Grafana JSON present; org-wide soft

### L50 — Chaos/load testing
score: 1  glyph: △
evidence:
  - healthz/readyz + prometheus unit tests
  - justfile nextest/coverage recipes
  - MISSING: dedicated load/soak/chaos harness
gaps:
  - Add steady-state load/chaos tests with hypothesis assertions — effort: L
soft_goal_delta: unit/integration validation exists; no continuous chaos discipline

CLUSTER_TOTAL score=21/30 pct=70% grade=C
CLUSTER_DONE cluster=C05 repo=sharecli
