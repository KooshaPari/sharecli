CLUSTER_START cluster=C06 repo=sharecli pillars=L51-L60 date=2026-07-13

### L51 — Pinned dependencies
score: 3  glyph: ✓
evidence:
  - Cargo.lock:1 — lockfile present at repo root
  - .github/workflows/ci.yml:57 — cargo clippy/test/build use --locked
  - deny.toml:1 — cargo-deny licenses/sources/advisories policy
  - justfile:97 — local cargo deny check in quality gate
gaps:
  - Add lockfile freshness / cargo update --locked CI assertion — effort: M
soft_goal_delta: lockfile+verify path is strong; freshness automation remains

### L52 — Reproducible builds
score: 3  glyph: ✓
evidence:
  - docs/slsa.md:58 — reproducible builds section (SOURCE_DATE_EPOCH + repro-check)
  - scripts/repro-check.sh:1 — dual-build SHA-256 digest compare
  - justfile:171 — `just repro-check` unix local gate
  - .github/workflows/repro-check.yml:1 — CI repro-check on lockfile/source churn
  - .github/workflows/release.yml:93 — release build seeds SOURCE_DATE_EPOCH=0
gaps:
  - Cross-target bit-identical gate for linux+darwin release tarballs — effort: M
soft_goal_delta: host release binary repro-check CI+local; matrix tarballs still manual

### L53 — Build provenance
score: 2  glyph: ~
evidence:
  - .github/workflows/release-attestation.yml:15 — SLSA Build L2 job
  - .github/workflows/release-attestation.yml:83 — attest-build-provenance@v1
  - docs/slsa.md:11 — current target SLSA Build L2 documented as achieved
gaps:
  - Upgrade to SLSA L3 with hermetic/parameterless build + GitHub OIDC hardening — effort: L
soft_goal_delta: L2 attestation wired; L3 not yet

### L54 — Hermetic builds
score: 1  glyph: △
evidence:
  - justfile:147 — local CI simulation path (fmt/lint/test/build/audit/deny)
  - .github/workflows/ci.yml:1 — gated validation on PR/push
  - MISSING: no-network build enforcement — CI still fetches crates.io during build
gaps:
  - Enforce offline/hermetic builds (vendored deps or network-blocked runners) — effort: L
soft_goal_delta: CI gates strong; hermeticity not enforced

### L55 — Dependency confusion guard
score: 3  glyph: ✓
evidence:
  - deny.toml:32 — unknown-registry=deny + crates.io allow-registry + git allowlist
  - deny.toml:1 — post-W5.1 source policy documented (crates.io-only; mirror N/A)
  - .github/workflows/deny.yml:1 — PR+schedule cargo deny (sources+licenses+advisories)
  - .github/dependabot.yml:12 — daily cargo intake for reviewed lock churn
  - audit.toml:7 — yanked=warn aligns with audit.yml (no stale yanked=allow comment)
gaps:
  - Automated typosquatting name-similarity scanner beyond deny+review — effort: M
soft_goal_delta: registry scoped to crates.io; typosquatting via deny+Dependabot+review

### L56 — Container provenance
score: 1  glyph: △
evidence:
  - Containerfile:1 — multi-stage image build for sharecli
  - docs/slsa.md:111 — cosign/GHCR verify roadmap documented (no secrets yet)
  - .github/workflows/security.yml:86 — container Trivy job gated on Dockerfile presence
  - MISSING: cosign sign/attest for published images (GHCR publish not wired)
gaps:
  - Wire GHCR publish + cosign sign/attest/verify on deploy — effort: L
soft_goal_delta: Containerfile+roadmap present; image signing not wired

### L57 — MCP server provenance
score: 0  glyph: ✗
evidence:
  - NOT_APPLICABLE-leaning: sharecli is a process supervisor CLI, not an MCP host
  - MISSING: phenoMCP / MCP server pin manifest — rg over repo manifests found no MCP provenance pins
gaps:
  - If MCP surfaces are added later, pin+attest server versions — effort: M
soft_goal_delta: n/a / not started for this repo type

### L58 — License scan
score: 3  glyph: ✓
evidence:
  - deny.toml:1 — cargo-deny license allow-list configured
  - .github/workflows/deny.yml:1 — scheduled + PR cargo deny check workflow
  - justfile:94 — local just deny recipe runs cargo deny check
gaps:
  - none identified for license allow-list enforcement path — effort: S
soft_goal_delta: every-PR + scheduled deny-on-violation path present

### L59 — Source code provenance
score: 1  glyph: △
evidence:
  - .github/CODEOWNERS:6 — curated ownership for core paths
  - AGENTS.md:47 — commit message format standardized
  - MISSING: required signed commits / DCO for all authors
gaps:
  - Require signed commits and branch protection for all authors — effort: L
soft_goal_delta: ownership explicit; commit-signing provenance absent

### L60 — Toolchain pinning
score: 3  glyph: ✓
evidence:
  - rust-toolchain.toml:1 — stable channel + rustfmt/clippy components pinned
  - mise.toml:1 — task/toolchain manager config present
  - Cargo.lock:1 — locked dependency resolution
  - .github/workflows/ci.yml — rust-toolchain / dtolnay align with pinned channel
gaps:
  - Pin exact rustc date/SHA in CI for bit-identical toolchain — effort: S
soft_goal_delta: rust-toolchain.toml landed (prior MISSING cleared)

CLUSTER_TOTAL score=20/30 pct=67% grade=C
CLUSTER_DONE cluster=C06 repo=sharecli
