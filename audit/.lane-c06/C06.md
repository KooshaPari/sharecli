CLUSTER_START cluster=C06 repo=sharecli pillars=L51-L60 date=2026-07-05

### L51 — Pinned dependencies
score: 3  glyph: ✓
evidence:
  - Cargo.lock:1 present at repo root
  - deny.toml:1 present for cargo-deny policy checks
  - justfile:95-96 runs `cargo deny check` in the local quality gate
gaps:
  - Add automated lockfile freshness enforcement in CI — effort: M
soft_goal_delta: lockfiles and deny policy are in place; verify-on-update is the last step

### L52 — Reproducible builds
score: 2  glyph: ~
evidence:
  - .github/workflows/release.yml:1 exists and handles release packaging
  - .github/workflows/ci.yml:1 exists for pre-release validation
  - docs/slsa.md:1 documents release provenance and reproducibility goals
gaps:
  - Add deterministic build checks with explicit repro comparison — effort: M
soft_goal_delta: release automation exists, but bit-identical verification is not yet explicit

### L53 — Build provenance
score: 2  glyph: ~
evidence:
  - docs/slsa.md:1 documents SLSA-oriented release provenance
  - .github/workflows/release.yml:1 exists as the release path
  - CHANGELOG.md:33-35 notes a full SLSA Build L2 attestation pipeline
gaps:
  - Upgrade from documented provenance to enforced attestations in CI — effort: L
soft_goal_delta: provenance is described and partially wired, but not proven everywhere

### L54 — Hermetic builds
score: 2  glyph: ~
evidence:
  - justfile:145-152 defines a local CI simulation path
  - .github/workflows/ci.yml:1 exists for gated validation
  - AGENTS.md:41-45 requires format/lint/test/type gates on PRs
gaps:
  - Enforce no-network build/runtime constraints in the workflow matrix — effort: L
soft_goal_delta: CI gates are strong, but hermeticity is not yet enforced as policy

### L55 — Dependency confusion guard
score: 2  glyph: ~
evidence:
  - deny.toml:1 exists for cargo-deny advisories/sources policy
  - Cargo.toml:1 uses a single workspace manifest
  - .github/dependabot.yml:1 exists for controlled update intake
gaps:
  - Add explicit registry scoping and typo-squat checks — effort: M
soft_goal_delta: dependency intake is controlled, but registry-level hardening is incomplete

### L56 — Container provenance
score: 1  glyph: △
evidence:
  - README.md:63-80 documents release archive installation for tray users
  - desktop/build.sh:1 exists for desktop packaging/build flow
  - docs/CROSS_DEVICE_DEPLOY.md:1 covers deployment packaging steps
gaps:
  - Add signed container/image attestations for shipped artifacts — effort: L
soft_goal_delta: release artifacts exist, but container signing/attestation is not visible

### L57 — MCP server provenance
score: 0  glyph: ✗
evidence:
  - src/lib.rs:1 and src/main.rs:1 expose the Rust CLI/runtime entrypoints
  - cargo metadata in Cargo.toml:1 defines crates, but no pinned MCP server manifest appears
  - rg for phenoMCP/server provenance terms only surfaces generic runtime and docs references
gaps:
  - Define and pin MCP server provenance metadata — effort: M
soft_goal_delta: no explicit MCP server pinning or attestation path is present

### L58 — License scan
score: 3  glyph: ✓
evidence:
  - deny.toml:1 configures cargo-deny license/advisory checks
  - gitleaks.toml:1 adds secret scanning policy alongside quality gates
  - justfile:95-96 and justfile:148-152 show the repo’s local check pipeline includes policy gates
gaps:
  - Add CI fail-closed enforcement for license violations if not already wired — effort: S
soft_goal_delta: license policy exists; the remaining gap is mainly automation coverage

### L59 — Source code provenance
score: 1  glyph: △
evidence:
  - .github/workflows/release.yml:1 exists for release publishing
  - .github/CODEOWNERS:1 curates ownership for core paths
  - AGENTS.md:47-51 standardizes commit message format, showing process discipline
gaps:
  - Require signed commits/branch protection for all authors — effort: L
soft_goal_delta: ownership is explicit, but commit-signing provenance is not visible

### L60 — Toolchain pinning
score: 2  glyph: ~
evidence:
  - mise.toml:1 pins the toolchain manager config
  - rustfmt.toml:1 and clippy.toml:1 pin formatter/linter settings
  - package.json:1 exists alongside Cargo.toml:1 for mixed-tooling repos
gaps:
  - Add explicit rust-toolchain.toml or equivalent SHA-pinned toolchain lock — effort: M
soft_goal_delta: tooling is configured, but the Rust toolchain itself is not explicitly pinned

CLUSTER_TOTAL score=18/30 pct=60% grade=C
CLUSTER_DONE cluster=C06 repo=sharecli
