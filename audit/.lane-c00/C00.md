CLUSTER_START cluster=C00 repo=sharecli pillars=L0-L9 date=2026-07-10

### L0 — Architecture Foundations
score: 2  glyph: ~
evidence:
  - SPEC.md documents layered CLI → Process/Project/Runtime/Monitoring → config → OS APIs (SPEC.md:4-19)
  - BOUNDARY.md locks ownership of process lifecycle vs thegent/tehgent (BOUNDARY.md:1-11)
  - Workspace members enumerate harness-native, ipc, fleet, fuse, thermal-tui, spawn-core-sys (Cargo.toml:85-96)
  - sharecli-core documents Hypervisor + ThermalGate + CoalesceCache architecture (crates/sharecli-core/src/lib.rs:1-37)
gaps:
  - No formal ports/adapters ADR; domain logic mixed with 100+ utility modules in src/lib.rs — effort: M
soft_goal_delta: architecture docs + crate split exist; hexagonal dep-arrows not enforced

### L1 — Module Structure & Boundaries
score: 2  glyph: ~
evidence:
  - Root lib exports focused runtime surface (ProcessPool, SharedRuntime, ProcessInfo) (src/lib.rs:22-26)
  - Commands live under src/commands/; Zig hot-core boundary documented in spawn_policy (src/spawn_policy.rs:1-39)
  - Dedicated crates for ipc, fleet, thermal-tui, harness-native strategies (Cargo.toml:86-96)
gaps:
  - src/lib.rs pub-mod lists dozens of unrelated utilities (bloom, bip39, dns, etc.) diluting module boundaries — effort: L
soft_goal_delta: real product modules are clear; utility sprawl weakens boundary discipline

### L2 — API Surface & Contract
score: 3  glyph: ✓
evidence:
  - Public CLI surface is clap-derived Commands enum (src/main.rs:42-80)
  - HTTP serve exposes `/`, `/healthz`, `/readyz`, `/config`, `/health/processes`, `/metrics/prometheus`, `/debug/pprof/profile`, `/ws` (src/commands/serve.rs)
  - docs/openapi/serve.yaml — OpenAPI 3.0.3 covering all serve routes
  - scripts/check-openapi-drift.py — symmetric route↔paths drift gate
  - .github/workflows/openapi-drift.yml — CI on serve/OpenAPI changes
gaps:
  - Versioned Stripe-style error envelope still absent — effort: M
soft_goal_delta: CLI + OpenAPI + drift CI; envelope backlog

### L3 — Data Model & State Management
score: 2  glyph: ~
evidence:
  - SPEC.md defines ProjectConfig, ResourceLimits, ProcessInfo, ProcessStatus, HealthStatus (SPEC.md:35-62)
  - runtime.rs implements ProcessInfo, ManagedProcess, ProcessPool with RwLock state (src/runtime.rs:41-80)
  - ProjectLimits + SharedRuntime resource tracking present (src/runtime.rs:439-481)
  - Config carries max_memory_mb / project_limits (src/config.rs:93-103,204-211)
gaps:
  - No schema migrations / durable session store despite FR-SESSION-002 persistence claim — effort: M
soft_goal_delta: in-memory domain model solid; persistence/state-machine rigor incomplete

### L4 — Async / concurrency design
score: 2  glyph: ~
evidence:
  - ProcessPool uses tokio::sync::RwLock and tokio::process::Command (src/runtime.rs:16-17,72-74)
  - serve uses broadcast + watch channels for thermal events and shutdown (src/commands/serve.rs:64-69,117)
  - Thermal poll task can request shutdown via watch Sender (src/commands/serve.rs:232-250)
gaps:
  - No CancellationToken / axum with_graceful_shutdown; EnvGuard uses set_var unsafely for spawn env (src/runtime.rs:27-38) — effort: M
soft_goal_delta: async shapes present; structured cancellation and shutdown incomplete

### L5 — Observability (logs / metrics / traces)
score: 2  glyph: ~
evidence:
  - main initializes tracing_subscriber::fmt with verbose→DEBUG (src/main.rs:370-373)
  - serve routes Prometheus metrics (src/commands/serve.rs:175,367-368)
  - In-process Counter/Gauge MetricsRegistry (src/metrics.rs:5-30)
  - Cargo.toml depends on tracing, tracing-subscriber, tracing-appender (Cargo.toml:30-32)
gaps:
  - No OpenTelemetry / #[instrument] on hot paths; LogSink is buffer-only (src/log_sink.rs:4-24) — effort: M
soft_goal_delta: logs+Prometheus seeded; distributed traces absent

### L6 — Performance budgets & profiling
score: 2  glyph: ~
evidence:
  - Cargo.toml — three Criterion `[[bench]]` targets (config_parse, pool_list, prometheus_render)
  - docs/ops/SLO.md — BENCH-1..3 budgets linked to harnesses
  - .github/workflows/bench.yml:56 — `bench-gate` job fails on >50% mean regression
  - docs/eval/baselines/criterion-baseline.json:1 — committed baseline means (SLO-seeded)
gaps:
  - Tighten threshold toward ~5–10%; add flamegraph/profiler CI artifacts — effort: M
soft_goal_delta: benches + hard-ish CI gate present; continuous profiler / tight budgets absent

### L7 — Concurrency safety & races
score: 1  glyph: △
evidence:
  - harness-native circuit_breaker uses AtomicU32/AtomicU64 with SeqCst (crates/harness-native/src/strategies/circuit_breaker.rs:6-29)
  - serve_lock + fs2 used for single-instance serve coordination (src/commands/serve.rs:11)
  - MISSING: loom/shuttle/miri/TSan jobs or TLA+ specs (grep returned 0 concurrency-model hits)
gaps:
  - Add loom tests for ProcessPool / serve_lock and document lock ordering — effort: M
soft_goal_delta: atomics exist on one path; formal race tooling absent

### L8 — Memory & allocation
score: 1  glyph: △
evidence:
  - spawn_policy documents Zig hot-core explicit allocator / no-panic alloc failure (src/spawn_policy.rs:28-29)
  - Tray package uses opt-level = "z" for size (Cargo.toml:81-83)
  - MISSING: #[global_allocator] / jemalloc / dhat / CI RSS budgets (grep returned 0)
gaps:
  - Adopt jemallocator on long-running serve binary + dhat CI sample — effort: S
soft_goal_delta: Zig notes only; Rust daemon memory discipline not started

### L9 — Build & release pipeline
score: 2  glyph: ~
evidence:
  - cargo-dist workspace metadata targets multi-OS binaries (Cargo.toml:105-118)
  - release.yml builds/publishes with concurrency + OIDC attestations permissions (./.github/workflows/release.yml:30-37,46-50)
  - docs/slsa.md claims SLSA Build L2 via release-attestation (docs/slsa.md:9-19,33-34)
  - .github/workflows/sbom.yml:19 — CycloneDX SBOM job uploads sharecli.cdx.json on main
  - Cargo.lock committed for locked builds
gaps:
  - Attach SBOM into release tarball; add SOURCE_DATE_EPOCH / bit-identical repro — effort: M
soft_goal_delta: release+attestation+SBOM CI present; repro/L3 incomplete

CLUSTER_TOTAL score=19/30 pct=63% grade=C
CLUSTER_DONE cluster=C00 repo=sharecli
