CLUSTER_START cluster=C00 repo=sharecli pillars=L0-L9 date=2026-07-18

### L0 — Architecture Foundations
score: 2  glyph: ~
evidence:
  - SPEC.md documents layered CLI → Process/Project/Runtime/Monitoring → config → OS APIs (SPEC.md:4-19)
  - BOUNDARY.md locks ownership of process lifecycle vs thegent/tehgent (BOUNDARY.md:1-11)
  - Workspace members enumerate harness-native, ipc, fleet, fuse, thermal-tui, spawn-core-sys (Cargo.toml:85-96)
  - sharecli-core documents Hypervisor + ThermalGate + CoalesceCache architecture (crates/sharecli-core/src/lib.rs:1-37)
  - docs/ops/lib-sprawl-plan.md Phase 1 util facade landed (Tier C under `src/util/`)
gaps:
  - No formal ports/adapters ADR; crate-split Phases 2–4 still pending — effort: M
soft_goal_delta: architecture docs + util facade; hexagonal dep-arrows not enforced until Phase 4

### L1 — Module Structure & Boundaries
score: 3  glyph: ✓
evidence:
  - Root lib exports focused Tier A/B surface + `pub mod util` only (src/lib.rs)
  - Tier C (~145 parity modules) namespaced under `sharecli::util` via `src/util/mod.rs` `#[path]`
  - tests/c00_lib_sprawl_facade.rs — FR-003 root pub-mod allowlist + util sample smoke
  - Commands live under src/commands/; Zig hot-core boundary documented in spawn_policy (src/spawn_policy.rs:1-39)
  - Dedicated crates for ipc, fleet, thermal-tui, harness-native strategies (Cargo.toml:86-96)
gaps:
  - Phase 2+ crate extract (`sharecli-parity`) still soft — effort: L
soft_goal_delta: root pub-mod sprawl closed by util facade; product modules clear at crate root

### L2 — API Surface & Contract
score: 3  glyph: ✓
evidence:
  - Public CLI surface is clap-derived Commands enum (src/main.rs:42-80)
  - HTTP serve exposes `/`, `/healthz`, `/readyz`, `/config`, `/health/processes`, `/metrics/prometheus`, `/debug/pprof/profile`, `/ws` (src/commands/serve.rs)
  - docs/openapi/serve.yaml — OpenAPI 3.0.3 covering all serve routes
  - scripts/check-openapi-drift.py — symmetric route↔paths drift gate
  - .github/workflows/openapi-drift.yml — CI on serve/OpenAPI changes
  - src/error_envelope.rs — shared `ErrorEnvelope` for serve auth + handlers (`docs/ops/error-envelope.md`)
  - tests/c00_serve_error_envelope.rs — golden 401 envelope bytes
gaps:
  - (none material; OpenAPI ErrorEnvelope component present)
soft_goal_delta: serve 4xx/5xx JSON unified; OpenAPI component present

### L3 — Data Model & State Management
score: 3  glyph: ✓
evidence:
  - SPEC.md defines ProjectConfig, ResourceLimits, ProcessInfo, ProcessStatus, HealthStatus (SPEC.md:35-62)
  - runtime.rs implements ProcessInfo, ManagedProcess, ProcessPool with RwLock state (src/runtime.rs:41-80)
  - ProjectLimits + SharedRuntime resource tracking present (src/runtime.rs:439-481)
  - Config carries max_memory_mb / project_limits (src/config.rs:93-103,204-211)
  - src/error_envelope.rs — typed `ErrorEnvelope` for serve 4xx/5xx JSON (`docs/ops/error-envelope.md`)
  - tests/c00_serve_error_envelope.rs — golden 401 envelope bytes
gaps:
  - No schema migrations / durable session store despite FR-SESSION-002 persistence claim — effort: M
soft_goal_delta: in-memory domain model + serve error contract solid; persistence rigor incomplete

### L4 — Async / concurrency design
score: 3  glyph: ✓
evidence:
  - ProcessPool uses tokio::sync::RwLock and tokio::process::Command (src/runtime.rs:16-17,72-74)
  - serve uses broadcast + watch channels for thermal events and shutdown (src/commands/serve.rs:64-69,117)
  - Thermal poll task can request shutdown via watch Sender (src/commands/serve.rs:232-250)
  - src/shutdown.rs — `CancellationToken` root + `serve_shutdown_signal` (Ctrl-C / thermal watch)
  - src/commands/serve.rs — `axum::serve(...).with_graceful_shutdown(...)`; child tokens for thermal + config reload
  - src/runtime.rs — spawn env overrides scoped in `spawn_blocking` + RAII `EnvGuard` (no set_var across `.await`)
  - docs/ops/async-shutdown.md — operator shutdown graph + spawn env contract
  - tests/c00_l4_async_shutdown_gate.rs — FR-003 evidence gate
gaps:
  - Full async ProcessPool under loom; OS-level cgroup shutdown hooks — effort: M
soft_goal_delta: structured CancellationToken shutdown + graceful HTTP drain; spawn env no longer spans async yield

### L5 — Observability (logs / metrics / traces)
score: 2  glyph: ~
evidence:
  - main initializes tracing_subscriber::fmt with verbose→DEBUG (src/main.rs:370-373)
  - serve routes Prometheus metrics (src/commands/serve.rs:175,367-368)
  - In-process Counter/Gauge MetricsRegistry (src/metrics.rs:5-30)
  - Cargo.toml depends on tracing, tracing-subscriber, tracing-appender (Cargo.toml:30-32)
gaps:
  - No OpenTelemetry / #[instrument] on hot paths; LogSink is buffer-only (src/log_sink.rs:4-24) — effort: M
soft_goal_delta: logs+Prometheus seeded; distributed traces absent

### L6 — Performance budgets & profiling
score: 3  glyph: ✓
evidence:
  - Cargo.toml — three Criterion `[[bench]]` targets (config_parse, pool_list, prometheus_render)
  - docs/ops/SLO.md — BENCH-1..3 budgets linked to harnesses
  - docs/ops/perf-budgets.md — 10% `bench-gate` contract + profiler artifact map
  - .github/workflows/bench.yml — `bench-gate` fails on >10% mean regression; uploads Criterion profiler JSON
  - docs/eval/baselines/criterion-baseline.json — `default_max_regression: 0.10`
  - scripts/check-bench-baseline.py — threshold default from baseline JSON
  - docs/eval/baselines/criterion-baseline.json:1 — committed baseline means (SLO-seeded)
gaps:
  - Continuous flamegraph SVG in CI (pprof protobuf only on serve); 5% gate after more nightly rows — effort: S
soft_goal_delta: 10% Criterion gate + CI profiler artifacts; serve pprof documented separately

### L7 — Concurrency safety & races
score: 3  glyph: ✓
evidence:
  - harness-native circuit_breaker uses AtomicU32/AtomicU64 with SeqCst (crates/harness-native/src/strategies/circuit_breaker.rs:6-29)
  - serve_lock + fs2 used for single-instance serve coordination (src/commands/serve.rs:11)
  - docs/ops/concurrency.md — lock ordering + loom hard gate contract
  - crates/sharecli-sync/src/lib.rs — sync pid map modeled after ProcessPool `processes` RwLock
  - crates/sharecli-sync/tests/loom_pool_index.rs — loom models concurrent insert/remove + RelaxedCounter inc
  - .github/workflows/ci.yml — `loom` job required via `ci-success` (T-670)
  - tests/c00_l7_loom.rs — FR-003 evidence gate
  - .github/workflows/miri-soft.yml — soft cargo miri on harness-native
gaps:
  - Full async ProcessPool + serve_lock under loom; CancellationToken shutdown — effort: M
soft_goal_delta: pid registry + metrics atomics loom-hard; async pool paths remain soft

### L8 — Memory & allocation
score: 3  glyph: ✓
evidence:
  - spawn_policy documents Zig hot-core explicit allocator / no-panic alloc failure (src/spawn_policy.rs:28-29)
  - Tray package uses opt-level = "z" for size (Cargo.toml:81-83)
  - docs/ops/memory.md — RSS budgets + jemalloc/dhat feature contract
  - docs/ops/alloc-profiling.md — jemalloc + dhat soft recipes wired
  - src/alloc.rs — `jemalloc` / `dhat-heap` `#[global_allocator]` + `active_allocator_label()`
  - Cargo.toml — `jemalloc` + `dhat-heap` features; `tikv-jemallocator` 0.7 optional dep
  - Containerfile — `cargo build --features jemalloc` for serve image
  - .github/workflows/rss-soft.yml — soft idle RSS (jemalloc build)
  - .github/workflows/dhat-soft.yml — soft dhat heap sample
  - scripts/ops/dhat_soft.sh + `just dhat-soft` / `just serve-jemalloc`
  - tests/c00_l8_allocator.rs — FR-003 feature + docs gate
gaps:
  - Hard RSS merge gate after jemalloc soak on main — effort: S
soft_goal_delta: feature-gated jemalloc for serve + soft dhat CI; hard RSS gate deferred

### L9 — Build & release pipeline
score: 3  glyph: ✓
evidence:
  - cargo-dist workspace metadata targets multi-OS binaries (Cargo.toml:105-118)
  - release.yml builds/publishes with concurrency + OIDC attestations permissions (./.github/workflows/release.yml:30-37,46-50)
  - docs/slsa.md claims SLSA Build L2 via release-attestation (docs/slsa.md:9-19,33-34)
  - .github/workflows/sbom.yml:19 — CycloneDX SBOM job uploads sharecli.cdx.json on main
  - .github/workflows/release.yml:106-136 — cargo-cyclonedx + sharecli.cdx.json copied into release archive stage
  - .github/workflows/release.yml:100-101 — SOURCE_DATE_EPOCH reproducibility seed
  - tests/c00_l9_sbom_release_gate.rs — FR-003 SBOM-in-release + sbom.yml CI evidence gate
  - Cargo.lock committed for locked builds
gaps:
  - Bit-identical repro verification; SLSA Build L3 network-blocked hermetic — effort: M
soft_goal_delta: SBOM in-archive + CI emission + SOURCE_DATE_EPOCH + attestation; L3 repro deferred

CLUSTER_TOTAL score=29/30 pct=97% grade=A
CLUSTER_DONE cluster=C00 repo=sharecli
