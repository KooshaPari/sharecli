CLUSTER_START cluster=C09 repo=sharecli pillars=L81-L95 date=2026-07-05

### L81.1 — WCAG 2.2 Level A Baseline
score: 1  glyph: △
evidence:
  - README.md:14-61 documents CLI and tray usage, but no a11y scan target, CI scan job, or accessibility checklist
  - tests/ only contains cast/coordination/process tests; no tests/a11y or tests/accessibility directory
gaps:
  - Add an accessibility scan target and a small automated a11y suite for the tray/TUI surfaces — effort: M
soft_goal_delta: basic user-facing docs exist, but zero enforced Level A coverage is visible

### L81.2 — WCAG 2.2 Level AA Color Contrast
score: 1  glyph: △
evidence:
  - crates/sharecli-thermal-tui/src/lib.rs:188-260 uses named `Color` values and semantic states for thermal levels and gate decisions
  - no contrast-related docs or CI checks surfaced in repo search output
gaps:
  - Document contrast ratios or theme tokens and wire a contrast check into CI — effort: M
soft_goal_delta: semantic colors are present, but contrast is undocumented and unverified

### L82.1 — Keyboard Navigation & Focus Management
score: 1  glyph: △
evidence:
  - desktop/ShareCLITray/Package.swift and desktop/include/sharecli_ffi.h show a native tray app and IPC surface, but no keyboard-nav or focus-test files were found
  - tests/ has no keyboard-nav or e2e focus coverage; only process/cast tests are present
gaps:
  - Add keyboard navigation coverage for the tray UI and verify focus behavior around dialogs/popovers — effort: M
soft_goal_delta: native UI exists, but keyboard operability is not exercised in-repo

### L82.2 — Predictable Interaction & State Feedback
score: 2  glyph: ~
evidence:
  - README.md:14-61 lists stable verbs like `config init`, `project add`, `project list`, `ps`, `status`, `start`, `stop`, and `prune`
  - FUNCTIONAL_REQUIREMENTS.md:1-80 defines process management expectations and resource/lifecycle behavior for the control plane
gaps:
  - Add state-transition tests or docs that show each command’s success/failure feedback path — effort: S
soft_goal_delta: primary commands and status feedback are discoverable, though not yet fully validated end-to-end

### L82.3 — Clear Error Messages & Recovery Guidance
score: 2  glyph: ~
evidence:
  - FUNCTIONAL_REQUIREMENTS.md:1-80 describes failure-aware orchestration, lifecycle management, and resource limits
  - README.md:63-145 includes install and verification steps, giving users a recovery path after setup
gaps:
  - Surface explicit recovery hints for common CLI failures in docs or command output — effort: S
soft_goal_delta: recovery language exists, but the CLI error surface itself is not fully documented

### L82.4 — Discoverable Help & Onboarding
score: 2  glyph: ~
evidence:
  - README.md:14-61 provides a quick-start command set for config, project, ps, status, start, stop, and prune
  - CLAUDE.md:1-20 gives a repository-specific agent entrypoint and working conventions
gaps:
  - Add a concise `sharecli --help`/docs excerpt or command reference table for new users — effort: S
soft_goal_delta: onboarding is present, but command discovery could be more explicit

### L82.6 — Touch / Pointer Target Usability
score: 0  glyph: ✗
evidence:
  - desktop/ShareCLITray/Package.swift indicates a native tray target, but no pointer-target sizing or hit-area docs surfaced
  - no visual specs or screenshot tests were found in the visible repo slice
gaps:
  - Document tray hit-area expectations or add UI snapshot coverage for pointer usability — effort: M
soft_goal_delta: pointer ergonomics are unmeasured in the current repo evidence

### L83.1 — Accessible Status & Progress Communication
score: 2  glyph: ~
evidence:
  - README.md:29-60 exposes `ps`, `status`, `check`, `optimize`, and `prune`, which all communicate current system state
  - crates/sharecli-thermal-tui/src/lib.rs:197-250 prints semantic status labels for pressure, gate decision, and back-pressure
gaps:
  - Add examples showing how status output reads in both normal and degraded modes — effort: S
soft_goal_delta: status-first design is strong, but alt-text-like redundancy for state is not documented

### L83.2 — Non-Color Redundancy
score: 1  glyph: △
evidence:
  - crates/sharecli-thermal-tui/src/lib.rs:215-219 includes textual labels like GREEN/YELLOW/RED alongside color, which is good redundancy
  - grep output showed no explicit non-color terminal fallback such as `NO_COLOR` or `TERM=dumb`
gaps:
  - Add non-color terminal handling and document it for headless or monochrome environments — effort: S
soft_goal_delta: semantic labels exist, but color-independent fallback is not explicit

### L83.3 — Documentation Readability & Structure
score: 2  glyph: ~
evidence:
  - README.md:1-145 is well sectioned with Purpose, Quick Start, Install/Launch, and Verify the install
  - FUNCTIONAL_REQUIREMENTS.md:1-80 gives a concise product summary and lifecycle scope
gaps:
  - Add a short command reference or troubleshooting section for top failure modes — effort: S
soft_goal_delta: docs are structured and readable, but could be more scan-friendly for operators

### L83.4 — Cognitive Load & Terminology Consistency
score: 2  glyph: ~
evidence:
  - README.md:7-12 and 14-61 keep a consistent process-manager vocabulary across commands and surface names
  - crates/sharecli-thermal-tui/src/lib.rs:197-250 uses consistent thermal/gate terminology in the UI copy
gaps:
  - Add a glossary or term map for project/harness/process distinctions — effort: S
soft_goal_delta: terminology is mostly consistent, but a glossary would reduce onboarding friction

### L84.1 — Screen-Reader / Semantics Support
score: 0  glyph: ✗
evidence:
  - no accessibility semantics, ARIA, or screen-reader annotations surfaced in grep output
  - desktop/ShareCLITray sources were visible, but no semantics API usage was found in the searched files
gaps:
  - Add semantics/documented accessibility labels for the tray UI if it is intended for assistive tech — effort: M
soft_goal_delta: no screen-reader support evidence surfaced

### L85.1 — Inclusive Feedback for State Changes
score: 2  glyph: ~
evidence:
  - crates/sharecli-thermal-tui/src/lib.rs:203-223 renders explicit pressure state plus prose feedback for green/yellow/red conditions
  - README.md:29-60 surfaces command verbs that map to clear process actions
gaps:
  - Add consistent success/failure wording examples for the main CLI commands — effort: S
soft_goal_delta: state-change feedback is present, but not yet standardized across commands

### L85.2 — Alternate Representations of Visual State
score: 1  glyph: △
evidence:
  - crates/sharecli-thermal-tui/src/lib.rs:215-219 and 235-245 pair color with text labels and decision copy
gaps:
  - Document alternate text/labels for any icon-only tray states — effort: S
soft_goal_delta: the TUI already offers textual redundancy, but tray icon fallback is undocumented

### L85.3 — Recovery Paths from Degraded State
score: 2  glyph: ~
evidence:
  - README.md:141-145 gives a simple verification path after install
  - README.md:59-60 and FUNCTIONAL_REQUIREMENTS.md:1-80 indicate process pruning and lifecycle control for cleanup/recovery
gaps:
  - Add a short troubleshooting section mapping common degraded states to recovery commands — effort: S
soft_goal_delta: recovery exists, but not yet packaged as a dedicated operator flow

### L85.4 — Error Recovery Discoverability
score: 1  glyph: △
evidence:
  - README.md:63-145 provides install verification, but not a troubleshooting matrix
  - tests/ lacks docs-driven recovery scenarios or error-state snapshots
gaps:
  - Create a terse troubleshooting appendix with recovery commands for the top 3 failures — effort: M
soft_goal_delta: the repo has recovery mechanisms, but they are not easy to discover

### L85.5 — User-Controlled Granularity
score: 2  glyph: ~
evidence:
  - README.md:14-61 exposes targeted commands like `project list`, `ps`, `status`, `start`, `stop`, `prune`, and `limits set`
  - FUNCTIONAL_REQUIREMENTS.md:1-80 and SPEC.md:1-120 describe process, resource, and lifecycle controls at a granular level
gaps:
  - Add examples showing small-scope vs bulk operations so users can choose the right granularity — effort: S
soft_goal_delta: operators can choose the scope, but the docs don’t spell out the tradeoffs

### L85.6 — Polite Failure & No Dead Ends
score: 2  glyph: ~
evidence:
  - crates/sharecli-thermal-tui/src/lib.rs:241-245 gives a retry-oriented explanation instead of a hard stop
  - README.md:141-145 ensures a basic verification path after install
gaps:
  - Standardize the fallback phrasing for CLI errors so failure messaging stays polite and actionable — effort: S
soft_goal_delta: failure messages are already recovery-oriented in the TUI slice

CLUSTER_TOTAL score=25/42 pct=60% grade=D
CLUSTER_DONE cluster=C09 repo=sharecli
