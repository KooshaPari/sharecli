CLUSTER_START cluster=C01 repo=sharecli pillars=L10-L19 date=2026-07-08

### L10 — CI/CD hygiene
score: 3  glyph: ✓
evidence:
  - ci.yml declares concurrency cancel-in-progress and contents:read permissions (./.github/workflows/ci.yml:14-19)
  - checkout / rust-cache / setup-node / upload-artifact / github-script pinned to commit SHAs across workflows (Wave10)
  - `ubuntu-24.04` replaces `ubuntu-latest` across `.github/workflows/*.yml`
gaps:
  - `dtolnay/rust-toolchain@stable` intentionally floats with the stable channel — effort: n/a
soft_goal_delta: matrix-wide SHA pins + ubuntu-24.04; rust-toolchain@stable left floating by design

### L11 — Test coverage & quality gates
score: 3  glyph: ✓
evidence:
  - ci.yml runs cargo test --locked --all-features (./.github/workflows/ci.yml:72-75)
  - quality-gate hard-enforces COVERAGE_THRESHOLD: 85 with cargo llvm-cov --fail-under-lines (./.github/workflows/quality-gate.yml:13,59-68)
  - coverage.yml emits llvm-cov JSON + LCOV and retains a SHA-keyed compact coverage snapshot artifact (./.github/workflows/coverage.yml)
  - TEST_COVERAGE_MATRIX.md pins Wave17 `--lib` coverage **77.34%** lines @ `fa887e9` (local run `local-lib-20260827`); Wave18 T-910 workspace-broad pin **82.38%** @ `d152eda` (WSL Linux run 2026-09-03) supersedes prior **80.51%** @ `5d8dc08`
  - audit/coverage-snapshots/fa887e9.coverage-snapshot.json — retained lib-pin llvm-cov totals (lines/functions/regions; local run `local-lib-20260827`)
  - audit/coverage-snapshots/d152eda.coverage-snapshot.json — Wave18 workspace-broad pin **82.38%** @ `d152eda` (retained; supersedes 80.51%)
  - audit/coverage-snapshots/5d8dc08.coverage-snapshot.json — prior workspace pin retained as historical evidence
  - audit/coverage-snapshots/8c68bb5.coverage-snapshot.json — earlier pin retained as historical evidence
  - audit/coverage-snapshots/d3cb7c4.coverage-snapshot.json — earlier pin retained as historical evidence
  - scripts/coverage_snapshot.py extracts machine-readable totals and target status from llvm-cov export JSON
  - tests/c01_coverage_pin_gate.rs — FR-003 gate for matrix pin + snapshot artifact + coverage.yml
  - justfile exposes test / test-nextest / ci recipes (justfile:63,68,147)
gaps:
  - Raise `--lib` line % toward 85% without lowering unit gate; `--workspace --all-features broad` remeasure unblocked on WSL/Linux (Wave18 T-910) — effort: M
soft_goal_delta: 85% unit gate hard; `--lib` percentage pinned at **77.34%** @ `fa887e9`; workspace-broad **82.38%** @ `d152eda` (Wave18) supersedes prior 80.51% @ `5d8dc08`

### L12 — Documentation & SSOT
score: 3  glyph: ✓
evidence:
  - README documents install, features, and observability surface (README.md:1-17,19-29)
  - SPEC.md is the technical SSOT for architecture and CLI commands (SPEC.md:1-31,65-80)
  - FUNCTIONAL_REQUIREMENTS.md lists FR-001..005 with on-disk Acceptance test refs (FUNCTIONAL_REQUIREMENTS.md:26-116)
  - docs/specs/TRACEABILITY.md — FR↔source↔tests matrix; 0 gaps (docs/specs/TRACEABILITY.md:23-32)
  - TEST_COVERAGE_MATRIX.md — FR-001..005 **Covered** rows; no TBD stubs (TEST_COVERAGE_MATRIX.md:85-97)
  - tests/c01_fr_ssot_gate.rs — FR-003 gate for FR index, TRACEABILITY, matrix parity
  - AGENTS.md + CONTRIBUTING.md + BOUNDARY.md cross-link project rules (AGENTS.md:1-8; CONTRIBUTING.md:6-8)
gaps:
  - Promote FR-CAST-00N extension IDs into docs/specs/FR.md as FR-006+ — effort: M
soft_goal_delta: FR↔test SSOT closed for Phase-3 FR-001..005; cast extensions still informal

### L13 — Onboarding & Contributor DX
score: 3  glyph: ✓
evidence:
  - CONTRIBUTING.md covers setup, quality gates, style, commits, testing, release (CONTRIBUTING.md:12-26)
  - mise.toml + justfile provide one-command fmt/lint/test/ci (justfile:147; mise.toml present)
  - README Quick install via cargo install / cargo-binstall (README.md:19-28)
  - AGENTS.md states test-first and quality-gate rules for agents (AGENTS.md:33-45)
gaps:
  - Add rust-toolchain.toml pin for hermetic onboarding — effort: S
soft_goal_delta: contributor path is complete; toolchain pin is soft polish

### L14 — Error handling & user feedback
score: 3  glyph: ✓
evidence:
  - src/error.rs — SharecliError (thiserror) + stable ErrorCode + sysexits-style exit constants
  - src/main.rs — run() maps domain errors to SHARECLI_ERROR_CODE stderr + process exit code
  - src/config_validator.rs — report_and_exit uses crate::error::EXIT_CONFIG (78)
  - tests/c01_l14_error_handling_gate.rs — FR-003 gate for thiserror + exit wiring
gaps:
  - Migrate remaining anyhow-only command paths to SharecliError variants — effort: M
soft_goal_delta: typed CLI error taxonomy + exit codes landed; HTTP uses error_envelope separately

### L15 — CLI/UX & ergonomics
score: 3  glyph: ✓
evidence:
  - clap derive CLI with subcommands, verbose/quiet, theme flag (src/main.rs:42-64)
  - Shell completions via clap_complete (src/main.rs:6; Cargo.toml:22)
  - Thermal TUI subcommand with DEFAULT_SLOT_CAP (src/main.rs:215; crates/sharecli-thermal-tui)
  - README documents hot-reload, notifications, completions, Prometheus (README.md:39-45)
gaps:
  - Align clap version string (0.1.0) with Cargo.toml 0.3.0 — effort: S
soft_goal_delta: CLI ergonomics meet acceptance; version string drift is soft

### L16 — Internationalization (i18n / l10n)
score: 1  glyph: △
evidence:
  - docs/adr/0003-cli-english-primary.md:1 — accepted English-primary / localization deferred ADR
  - User-facing strings are English literals in clap about/help (src/main.rs)
  - MISSING: fluent/gettext catalogs (deferred by ADR until multi-locale need)
gaps:
  - Add fluent/gettext only if multi-locale becomes a product goal — effort: L
soft_goal_delta: policy seeded (English-primary ADR); catalogs deferred

### L17 — Accessibility (a11y)
score: 3  glyph: ✓
evidence:
  - docs/a11y/README.md — WCAG 2.2 Level A / CLI-TUI Level A posture + degraded modes
  - docs/a11y/cli-tui-checklist.md — pre-merge operator checklist (color, NO_COLOR, keyboard, landmarks)
  - docs/a11y/contrast.md + keyboard.md + status-and-recovery.md — supporting surfaces
  - .github/workflows/a11y.yml — axe Level A hard-fail on serious/critical
  - Brand SVG role=img / aria-label; macOS tray accessibilityDescription
gaps:
  - Manual SR quarterly pass logged in CHANGELOG — effort: S
soft_goal_delta: CLI/TUI checklist + dashboard axe gate complete

### L18 — Secret management
score: 3  glyph: ✓
evidence:
  - security.yml runs gitleaks-action on SHA pin (./.github/workflows/security.yml:31)
  - gitleaks.toml defines JWT/OAuth token rules (gitleaks.toml:55,117-120)
  - .env.example documents SHARECLI_SERVE_* placeholders without real secrets (.env.example)
  - docs/ops/secrets.md — runtime contract for bearer + JWT env (SHARECLI_SERVE_TOKEN, JWKS, rotation)
  - src/serve_auth.rs — env precedence + SHA-256 bearer compare + JWT JWKS validation
  - tests/c01_l18_secrets_gate.rs — FR-003 gate for secrets runbook + serve_auth parity
gaps:
  - OS keyring helper for serve token on shared hosts — effort: M
soft_goal_delta: scanning + runtime secret contract documented and gated; keyring deferred

### L19 — Supply-chain security (SBOM / CVE / lockfile pinning)
score: 3  glyph: ✓
evidence:
  - Cargo.lock committed; CI uses --locked (./.github/workflows/ci.yml)
  - deny.toml enforces licenses, deny unknown-git, advisory ignores with reasons
  - SECURITY.md lists cargo audit, deny, scorecard, Dependabot, CodeQL
  - .github/workflows/sbom.yml — CycloneDX `sharecli.cdx.json` artifact on main
  - .github/workflows/release.yml — SBOM embedded in release stage archives
gaps:
  - Fail CI on unignored advisories once backlog cleared — effort: M
soft_goal_delta: lockfile+deny+audit+CycloneDX SBOM in CI/release

CLUSTER_TOTAL score=28/30 pct=93% grade=A
CLUSTER_DONE cluster=C01 repo=sharecli
