CLUSTER_START cluster=C02 repo=sharecli pillars=L20-L29 date=2026-07-08

### L20 — Threat model & attack surface
score: 3  glyph: ✓
evidence:
  - THREAT_MODEL.md — STRIDE-per-component + HTTP/WS inventory + mitigations
  - SECURITY.md Threat Model table links to THREAT_MODEL.md
  - BOUNDARY.md defines process-orchestration trust boundary (BOUNDARY.md:1-11)
gaps:
  - Yearly review sign-off after federated AuthN lands — effort: S
soft_goal_delta: full STRIDE artifact present (score-3)

### L21 — AuthN / AuthZ
score: 3  glyph: ✓
evidence:
  - src/serve_auth.rs — Bearer + JWT/JWKS middleware; `/healthz` `/readyz` public
  - src/config.rs — `ServeJwtConfig` (issuer/audience/jwks_path|jwks); auth_mode open|bearer|jwt
  - src/commands/serve.rs — AuthN wiring; audit `auth_enabled` mode=jwt|bearer
  - docs/ops/AUTH.md — open / bearer / JWT federated IdP operator guide
  - tests/fr012_serve_jwt_auth.rs — FR-012 AC-012.1..012.3
gaps:
  - OAuth Authorization Code / SAML SP / unix-socket peer creds — effort: L
soft_goal_delta: federated JWT resource-server AuthN beyond static bearer

### L22 — Cryptography & key management
score: 2  glyph: △
evidence:
  - sha2 listed as dependency (Cargo.toml)
  - Utility crypto modules exist (xxtea, hkdf, chacha20, x509_chain, pem_decode) under src/
  - serve_auth compares SHA-256 digests of bearer tokens
  - docs/ops/crypto-keys.md — product key stance (no KMS; bearer digest only)
gaps:
  - Prefer audited crates for any real crypto; document key lifecycle or remove toy crypto from public CLI — effort: M
soft_goal_delta: crypto primitives seeded as utils; product key management absent

### L23 — Audit log & compliance
score: 3  glyph: ✓
evidence:
  - src/audit_log.rs — append-only JSONL + size rotation (`SHARECLI_AUDIT_MAX_BYTES` / `RETAIN`)
  - Events: `auth_ok` / `auth_fail` / `auth_enabled` / `serve_start` / `serve_stop`
  - docs/ops/AUTH.md — audit path + retention table
gaps:
  - Spawn/stop/config-change event coverage still partial — effort: S
soft_goal_delta: retention/rotation policy wired; compliance trail present

### L24 — Multi-tenant isolation & data privacy
score: 2  glyph: △
evidence:
  - ProjectLimits isolate per-project resource caps in SharedRuntime
  - BOUNDARY.md scopes ownership but not tenant isolation
  - docs/ops/privacy-tenant.md — single-operator local tenancy policy
gaps:
  - Document single-user local threat model explicitly — effort: M
soft_goal_delta: resource isolation seeded; privacy/tenant policy absent

### L25 — Resource limits & rate limits
score: 3  glyph: ✓
evidence:
  - ProjectLimits / max_memory_mb in config and runtime (src/config.rs:93-103; src/runtime.rs:439-445)
  - SpawnPolicy Zig semaphore throttles build harness concurrency (src/spawn_policy.rs:1-18)
  - RateLimiter sliding-window helper with tests (src/rate_limiter.rs:3-43)
  - src/serve_rate_limit.rs — config/env resolver + probe exemption
  - src/commands/serve.rs — `serve_rate_limit_middleware` (429 + Retry-After + ErrorEnvelope)
  - docs/ops/rate-limits.md — operator contract for `[serve]` rate_limit_* and env overrides
  - tests/c02_serve_rate_limit.rs — FR-003 gate (envelope, saturation, router wiring)
gaps:
  - Enforce OS-level cgroup/job-object limits end-to-end — effort: M
soft_goal_delta: HTTP rate limit wired on serve; ProjectLimits + spawn semaphore present; OS cgroup enforcement deferred

### L26 — Resilience (retries / backoff / circuit / bulkhead / timeouts / health)
score: 2  glyph: ~
evidence:
  - RetryPolicy + exponential delay helpers (src/retry.rs:3-21)
  - BackoffStrategy Fixed/Linear/Exponential (src/backoff.rs:3-16)
  - harness-native circuit_breaker opens after failure threshold (crates/harness-native/src/strategies/circuit_breaker.rs:13-29)
  - Thermal gate sleep-retry on RED (crates/sharecli-core/src/lib.rs:128-135,421)
  - /healthz liveness on serve (src/commands/serve.rs:3,172,274)
gaps:
  - No bulkhead/chaos tests; health lacks readiness vs liveness split — effort: M
soft_goal_delta: retry/circuit/thermal resilience present; chaos/bulkhead incomplete

### L27 — Observability of failure modes (SLO/SLA / error budget / burn / runbooks)
score: 3  glyph: ✓
evidence:
  - docs/ops/SLO.md — SLO-1..SLO-4 including AuthN burn (unauthorized rate)
  - docs/ops/alertmanager/sharecli.yml — SharecliAuthFailBurn + HTTP error burn
  - docs/ops/alerting.md — runbooks for AuthFailBurn / HealthzDown / ErrorBudgetBurn
  - src/http_red.rs — `sharecli_http_unauthorized_total` counter
gaps:
  - Live PagerDuty routing keys remain env-only (intentional) — effort: S
soft_goal_delta: burn alerts + runbooks present for AuthN and HTTP errors

### L28 — Dependency hygiene & CVEs
score: 3  glyph: ✓
evidence:
  - deny.toml licenses + sources deny unknown-git + justified advisory ignores (deny.toml:1-37)
  - audit.yml + deny.yml scheduled/PR workflows (SECURITY.md:82-85; ./.github/workflows/deny.yml:5-21)
  - Dependabot daily cargo updates (.github/dependabot.yml:12-14)
  - CI clippy/test use --locked (./.github/workflows/ci.yml:57,73)
gaps:
  - Clear or re-justify RUSTSEC ignores when upstreams upgrade — effort: S
soft_goal_delta: dependency CVE hygiene meets acceptance; ignore-list churn is soft

### L29 — Repository governance (license / CI / license-check / CODEOWNERS)
score: 3  glyph: ✓
evidence:
  - MIT LICENSE present (LICENSE:1-5)
  - CODEOWNERS assigns @KooshaPari across paths (.github/CODEOWNERS:1-40)
  - cargo-deny license policy enforced in CI (deny.toml:1-23; ./.github/workflows/deny.yml:1-4)
  - CODE_OF_CONDUCT.md + CONTRIBUTING.md + SECURITY.md governance set present
gaps:
  - Document required status checks / branch protection in-repo — effort: S
soft_goal_delta: governance artifacts complete; branch-protection visibility is soft

CLUSTER_TOTAL score=27/30 pct=90% grade=A
CLUSTER_DONE cluster=C02 repo=sharecli
